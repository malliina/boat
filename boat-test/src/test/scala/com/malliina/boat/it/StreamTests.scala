package com.malliina.boat.it

import com.malliina.boat.BoatNames
import play.api.libs.json.Json

class StreamTests extends BoatTests {
//  test("boats2") {
//    withWebSocket(reverse.updates2(), BoatNames.random(), json => println("listener got: " + json)) { _ =>
//      withWebSocket(reverse.boats2(), BoatNames.random(), json => println("boat got: " + json)) { socket =>
//        socket.sendMessage(Json.obj("msg" -> "test"))
//        Thread.sleep(2000)
//      }
//    }
//  }
}
